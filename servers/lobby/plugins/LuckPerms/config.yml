
storage-method: mysql

data:
  # host (or host:port) for the database. If you leave out :port it will
  # default to the engine default (MySQL/MariaDB: 3306)
  address: 127.0.0.1

  # The name of the database to store LuckPerms data in.
  database: luckperms

  # Credentials for the database.
  username: lp_user
  password: "GKPqCNVNZrMsPJEb"

  # Connection/pool settings (kept small for this environment)
  pool-settings:
    maximum-pool-size: 6
    minimum-idle: 1
    connection-timeout: 5000
    keepalive-time: 0
    maximum-lifetime: 1800000

  properties:
    useUnicode: true
    characterEncoding: utf8

table-prefix: 'luckperms_'

